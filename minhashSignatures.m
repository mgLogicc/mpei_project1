function [outputArg1,outputArg2] = minhashSignatures(shingles, numHash)
    %
    % 
    %
    %
    %
    %
    
    size = length(shingles);
    signatures = zeros(numHash,size);

    for hf = 1:numHash
        
        for shingleIdx

    end
    
end